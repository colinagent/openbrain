/** Rendered markdown inline code — flat bg matching code blocks, no border. */
export const CM_MD_INLINE_CODE = 'cm-md-inline-code';

/** Flat capsule pill for prompt vars — matches frontmatter list chips, not tag chips. */
export const CM_MD_INLINE_PILL = 'ui-capsule-pill cm-md-inline-pill';
